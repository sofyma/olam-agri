[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "18"

# Handle SvelteKit API routes
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/entry"
  status = 200

# Handle SPA routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200 